/*
 *标题 （包括初次加载雪花屏的标题 页面的标题 浏览器的标题）
 */
export const title: string = 'charling';
/**
 * antd库的css class前缀
 */
export const prefixCls = 'charling';
/**
 * 配置 webpack 的 publicPath。
 *
 * 当打包的时候，webpack 会在静态文件路径前面添加 publicPath 的值，
 * 当你需要修改静态文件地址时，比如使用 CDN 部署，把 publicPath 的值
 * 设为 CDN 的值就可以。
 * */
// export const publicPath = process.env.WE_CDN_STATUE === 'open' ? cdnPath : routePrefix;
